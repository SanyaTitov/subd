create table faculty (
    faculty_name VARCHAR(50) primary key,
    faculty_dekan VARCHAR(50) not null
);

insert into faculty (faculty_name, faculty_dekan) values
('ИВТ', 'Петров П.П.'),
('Экономика', 'Павлова О.И.');

create table Student3 (
    Student_ID int primary key,
    full_name VARCHAR(50) not null,
    group_number VARCHAR(5) not null,
    faculty_name VARCHAR(50) not null,
    foreign key (faculty_dekan) references faculty(faculty_dekan)
);

insert into Student (Student_ID, full_name, group_number, faculty_name faculty_dekan) values
(1, 'Иванов А.А.', '101', 'ИВТ', 'Петров П.П.'),
(2, 'Сидорова Б.Б.', '102', 'Экономика', 'Павлова О.И.'),
(3, 'Кузнецов В.В.', '101', 'ИВТ', 'Петров П.П');
